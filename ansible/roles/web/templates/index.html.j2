<!doctype html>
<html lang="fr">
<head>
  <meta charset="utf-8">
  <title>TP Terraform Ansible</title>
  <style>
    body {
      margin: 0;
      font-family: Arial, sans-serif;
      background: #f4f6fb;
      color: #1f2937;
    }

    main {
      max-width: 760px;
      margin: 80px auto;
      padding: 32px;
      background: white;
      border: 1px solid #d8deea;
      border-radius: 8px;
    }

    h1 {
      margin-top: 0;
      color: #3155a4;
    }

    code {
      background: #eef2ff;
      padding: 4px 8px;
      border-radius: 4px;
    }
  </style>
</head>
<body>
  <main>
    <h1>Serveur web configure avec Ansible</h1>
    <p>Nginx fonctionne correctement sur la VM web.</p>
    <p>Adresse IP de la base de donnees : <code>{{ hostvars['db']['ansible_host'] }}</code></p>
  </main>
</body>
</html>
